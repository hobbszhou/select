CMAKE_MINIMUM_REQUIRED(VERSION 3.10.2)
PROJECT(cli1)

MESSAGE(STATUS "${PROJECT_SOURCE_DIR}")

add_definitions("-std=c++11 -g -Wall ")

LINK_LIBRARIES(pthread)

AUX_SOURCE_DIRECTORY(. DIR_LIB_SRCS)

MESSAGE(STATUS "${PROJECT_SOURCE_DIR}")

ADD_EXECUTABLE(cli  ${DIR_LIB_SRCS})